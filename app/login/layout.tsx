export const metadata = {
    title: "Superhero Store - Login",
    description: "Login and manage your account",
}

export default function LoginLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}