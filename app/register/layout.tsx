
export const metadata = {
    title: "Superhero Store - Register",
    description: "Create your account and login!",
}

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}